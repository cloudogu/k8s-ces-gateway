apiVersion: v1
values:
  images:
    componentOperator: cloudogu/k8s-ces-gateway:0.0.1
patches:
  values.yaml:
    additionalImages:
    manager:
      image:
        registry: "{{ registryFrom .images.componentOperator }}"
        repository: "{{ repositoryFrom .images.componentOperator }}"
        tag: "{{ tagFrom .images.componentOperator }}"